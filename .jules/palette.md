## 2024-06-25 - Improve Loading State and Image Sizing
**Learning:** Using `st.spinner` provides critical visual feedback during synchronous model prediction in Streamlit, and `use_container_width=True` avoids conflicting responsive sizing.
**Action:** Added `st.spinner` to the Predict button and updated `st.image` parameters in `main.py`.
